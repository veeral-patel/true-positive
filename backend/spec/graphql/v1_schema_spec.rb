require 'rails_helper'

RSpec.describe V1Schema do 
    # uncomment me when the API is more stable!

    # it "dumped schema should be up to date" do
    #   actual_schema = V1Schema.to_definition
    #   dumped_schema = File.read(Rails.root.join("app/graphql/schema.graphql"))
  
    #   expect(actual_schema).to eq(dumped_schema), 'GraphQL schema is out of date. Run rails custom:dump_schema'
    # end
  end